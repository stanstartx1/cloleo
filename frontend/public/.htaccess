<IfModule mod_rewrite.c>
  RewriteEngine On

  # Serve real files and directories normally; hand React routes to the SPA.
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule ^ index.html [L]
</IfModule>
